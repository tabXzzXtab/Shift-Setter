# ---- Supabase project (client, safe to expose) ----
NEXT_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<ANON_PUBLIC_KEY>

# ---- Supabase CLI (never exposed to the browser) ----
SUPABASE_PROJECT_REF=<PROJECT_REF>
SUPABASE_ACCESS_TOKEN=<PERSONAL_ACCESS_TOKEN>

# ---- Direct database connection, for running arbitrary SQL in dev/test ----
# Copy verbatim from Supabase dashboard -> Connect -> ORM/psql.
# Password percent-encoded if it contains  @ : / ? # [ ] % &
SUPABASE_DB_PASSWORD=<DB_PASSWORD>
DATABASE_URL=postgresql://postgres:<DB_PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres

# ---- Walkthrough (scripts/walkthrough.mjs) ----
# The founding admin, created by scripts/bootstrap-admin.mjs.
WALKTHROUGH_ADMIN_EMAIL=
WALKTHROUGH_ADMIN_PASSWORD=

# ---- Stable demo logins (scripts/demo-accounts.mjs) ----
# Recreated after every demo reset, so a handed-out login keeps working.
# The admin survives the reset by itself, but nothing pins its password, so it
# belongs here too -- and it is NOT the founding admin above, whose identifier
# is half a credential and is not handed out.
DEMO_ADMIN_EMAIL=
DEMO_ADMIN_PASSWORD=
DEMO_LEADER_EMAIL=
DEMO_LEADER_PASSWORD=
DEMO_WORKER_EMAIL=
DEMO_WORKER_PASSWORD=
